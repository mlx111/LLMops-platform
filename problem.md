 ┌─────┬──────┬──────────────────────────────────────────┬─────────┬───────────────────────────────────────────────────────────────────────────────────────────┐  
  │  #  │ 类型 │                   文件                   │  行号   │                                         问题描述                                          │  
  ├─────┼──────┼──────────────────────────────────────────┼─────────┼───────────────────────────────────────────────────────────────────────────────────────────┤  
  │ 1   │ 安全 │ backend/app/tasks/celery_app.py          │ 409     │ SSRF 漏洞 — target_url 来自用户输入，未经任何校验直接传给                                 │  
  │     │      │                                          │         │ requests.post()，可被用于访问内部服务                                                     │
  ├─────┼──────┼──────────────────────────────────────────┼─────────┼───────────────────────────────────────────────────────────────────────────────────────────┤
  │ 2   │ Bug  │ backend/app/services/runner.py           │ 371-376 │ Token 计数双倍计算 — eval_overhead 已将 input_text + actual_output 计入，又被加到         │
  │     │      │                                          │         │ input_tokens 里，导致数据失真                                                             │
  ├─────┼──────┼──────────────────────────────────────────┼─────────┼───────────────────────────────────────────────────────────────────────────────────────────┤
  │ 3   │ 性能 │ backend/app/routers/runs.py              │ 168-198 │ 同步 Redis 阻塞事件循环 — pubsub.get_message(timeout=1.0) 在 async def                    │
  │     │      │                                          │         │ 中阻塞调用，多客户端时吞吐量急剧下降                                                      │
  ├─────┼──────┼──────────────────────────────────────────┼─────────┼───────────────────────────────────────────────────────────────────────────────────────────┤
  │ 4   │ Bug  │ backend/app/services/report_generator.py │ 233-235 │ if False 硬编码 — total_tokens 统计被 if False else "N/A" 永远跳过，成本汇总始终为 N/A    │
  ├─────┼──────┼──────────────────────────────────────────┼─────────┼───────────────────────────────────────────────────────────────────────────────────────────┤
  │ 5   │ Bug  │ backend/app/routers/runs.py              │ 42-44   │ Dataset 存在性校验错误 — 查的是 EvalCase 表而非 Dataset 表，空数据集无法创建 Run          │
  ├─────┼──────┼──────────────────────────────────────────┼─────────┼───────────────────────────────────────────────────────────────────────────────────────────┤
  │ 6   │ 性能 │ backend/app/routers/runs.py              │ 72-73   │ 同步执行阻塞 HTTP — Celery 不可用时降级为同步执行，HTTP 请求会挂起直到全部 Case 跑完      │
  └─────┴──────┴──────────────────────────────────────────┴─────────┴───────────────────────────────────────────────────────────────────────────────────────────┘

  中等问题（设计 / 代码质量）

  ┌─────┬──────┬──────────────────────────────────────────────────┬─────────┬──────────────────────────────────────────────────────────────────────────────────┐
  │  #  │ 类型 │                       文件                       │  行号   │                                     问题描述                                     │
  ├─────┼──────┼──────────────────────────────────────────────────┼─────────┼──────────────────────────────────────────────────────────────────────────────────┤
  │ 7   │ 安全 │ backend/app/services/provider_models.py          │ 96-102  │ API Key 明文在缓存 Key 中 — 已解码的密钥直接拼入内存缓存 Key                     │
  │     │      │                                                  │         │ 字符串，存在泄露风险                                                             │
  ├─────┼──────┼──────────────────────────────────────────────────┼─────────┼──────────────────────────────────────────────────────────────────────────────────┤
  │ 8   │ 并发 │ backend/app/services/provider_models.py          │ 15      │ 全局缓存无锁 — _provider_model_cache 是模块级 dict，多线程环境下存在数据竞争     │
  ├─────┼──────┼──────────────────────────────────────────────────┼─────────┼──────────────────────────────────────────────────────────────────────────────────┤
  │ 9   │ 设计 │ backend/app/routers/runs.py                      │ 169-194 │ SSE 初始状态可能丢失 — Redis Pub/Sub subscribe 到首次 get_message                │
  │     │      │                                                  │         │ 之间有微小窗口可能丢消息                                                         │
  ├─────┼──────┼──────────────────────────────────────────────────┼─────────┼──────────────────────────────────────────────────────────────────────────────────┤
  │ 10  │ Bug  │ frontend/src/pages/ExperimentConfig.tsx          │ 265-269 │ SSE 监听可能未建立 — 创建 Run 后查找 status === 'running'，但 Run 可能短暂为     │
  │     │      │                                                  │         │ pending 状态，导致监听器没挂上                                                   │
  ├─────┼──────┼──────────────────────────────────────────────────┼─────────┼──────────────────────────────────────────────────────────────────────────────────┤
  │ 11  │ 设计 │ frontend/src/pages/DatasetManagement.tsx         │ 25-26   │ Modal 共享 Form 实例 — "Add Case" 和 "Import JSON" 使用同一个                    │
  │     │      │                                                  │         │ caseForm，字段定义完全不同，验证状态可能混乱                                     │
  ├─────┼──────┼──────────────────────────────────────────────────┼─────────┼──────────────────────────────────────────────────────────────────────────────────┤
  │ 12  │ 性能 │ backend/app/models/run.py                        │ 53,63   │ 外键缺少索引 — EvalResult.run_id / case_id、Trace.run_id /                       │
  │     │      │                                                  │         │ case_id、TraceStep.trace_id_link 均无索引                                        │
  ├─────┼──────┼──────────────────────────────────────────────────┼─────────┼──────────────────────────────────────────────────────────────────────────────────┤
  │ 13  │ 工程 │ tests/test_*.py                                  │ 全部    │ 测试用 sys.path.insert — 所有测试文件用此方式添加路径，脆弱且并行时可能冲突      │
  ├─────┼──────┼──────────────────────────────────────────────────┼─────────┼──────────────────────────────────────────────────────────────────────────────────┤
  │ 14  │ 类型 │ backend/app/models/dataset.py:38 vs              │ 不一致  │ reference_context_ids 类型不一致 — 模型用 list，Schema 用                        │
  │     │      │ schemas/dataset.py:42                            │         │ list[str]，类型检查会报错                                                        │
  ├─────┼──────┼──────────────────────────────────────────────────┼─────────┼──────────────────────────────────────────────────────────────────────────────────┤
  │ 15  │ 设计 │ backend/app/routers/versions.py                  │ 55-62   │ 删除 Version 无级联检查 — 被 EvalRun 引用的 Version 删除时触发外键约束 500 错误  │
  └─────┴──────┴──────────────────────────────────────────────────┴─────────┴──────────────────────────────────────────────────────────────────────────────────┘

  小问题 / 优化建议

  ┌─────┬──────┬───────────────────────────────────────┬─────────┬───────────────────────────────────────────────────────────────────┐
  │  #  │ 类型 │                 文件                  │  行号   │                             问题描述                              │
  ├─────┼──────┼───────────────────────────────────────┼─────────┼───────────────────────────────────────────────────────────────────┤
  │ 16  │ 数据 │ backend/app/models/run.py             │ —       │ 缺少 (run_id, case_id) 唯一约束 — 理论上可插入重复的 EvalResult   │
  ├─────┼──────┼───────────────────────────────────────┼─────────┼───────────────────────────────────────────────────────────────────┤
  │ 17  │ 设计 │ backend/app/services/runner.py        │ 322     │ 错误信息截断过短 — str(e)[:200] 可能丢失 LLM 返回的完整错误上下文 │
  ├─────┼──────┼───────────────────────────────────────┼─────────┼───────────────────────────────────────────────────────────────────┤
  │ 18  │ UX   │ frontend/src/pages/Settings.tsx       │ 53-63   │ 保存 API Key 后未清空字段 — 重新打开 Modal 旧 Key 值仍可见        │
  ├─────┼──────┼───────────────────────────────────────┼─────────┼───────────────────────────────────────────────────────────────────┤
  │ 19  │ UX   │ frontend/src/pages/RunResults.tsx     │ 136-137 │ 详情弹窗无加载状态 — 异步加载结果时 Modal 直接打开但显示空白      │
  ├─────┼──────┼───────────────────────────────────────┼─────────┼───────────────────────────────────────────────────────────────────┤
  │ 20  │ UX   │ frontend/src/pages/VersionCompare.tsx │ —       │ 对比结果只显示 case_id — 看不出具体是哪个 Case 有变化             │
  └─────┴──────┴───────────────────────────────────────┴─────────┴───────────────────────────────────────────────────────────────────┘

  测试覆盖缺口

  ┌─────┬────────────────────────────────────────────────────┬──────────┐
  │  #  │                        场景                        │ 当前覆盖 │
  ├─────┼────────────────────────────────────────────────────┼──────────┤
  │ 21  │ SSE /runs/{id}/stream 端点（含 Redis/DB 两种模式） │ 缺失     │
  ├─────┼────────────────────────────────────────────────────┼──────────┤
  │ 22  │ _call_target_system 网络异常 / 超时处理            │ 缺失     │
  ├─────┼────────────────────────────────────────────────────┼──────────┤
  │ 23  │ Report 生成 API                                    │ 缺失     │
  ├─────┼────────────────────────────────────────────────────┼──────────┤
  │ 24  │ 创建 Run 时 target_url 传递                        │ 缺失     │
  ├─────┼────────────────────────────────────────────────────┼──────────┤
  │ 25  │ 前端组件渲染测试                                   │ 全部缺失 │
  └─────┴────────────────────────────────────────────────────┴──────────┘


 高危

  - /F:/longchain/第二个项目/llmops-platform/backend/app/models/apikey.py:33 把 API Key 用 Base64 存储，不是加密；/F:/longchain/第二个项目/llmops-platform/backend/
    app/services/runner.py:29 会解码回明文。数据库、备份或只读权限泄露时，密钥直接暴露。
  - /F:/longchain/第二个项目/llmops-platform/backend/app/main.py:15 允许 allow_origins=["*"] 且 allow_credentials=True。如果这个服务不只在纯本机开发环境跑，浏览器
    里的任意站点都可能借用户环境调用本地 API。
  - /F:/longchain/第二个项目/llmops-platform/backend/app/routers/config_router.py:61 允许把 base_url 直接写入数据库，随后 /F:/longchain/第二个项目/llmops-platform/
    backend/app/services/provider_models.py:99 会按这个地址去抓模型列表。这个是另一条 SSRF 面，和 target_url 无关，能打到任意内部地址。
  - /F:/longchain/第二个项目/llmops-platform/backend/app/services/url_safety.py:30 只在校验时解析域名，真正请求时 /F:/longchain/第二个项目/llmops-platform/backend/
    app/tasks/celery_app.py:416 仍然按域名发请求。这里有 DNS rebinding / TOCTOU 窗口，能绕过 SSRF 防护。

  中高危

  - /F:/longchain/第二个项目/llmops-platform/backend/app/tasks/celery_app.py:243 在没有 target_url 或没有 API Key 时会进入 demo 模式，结果仍然会被标记成
    completed。虽然前端已经加了提示，但 /F:/longchain/第二个项目/llmops-platform/backend/app/services/report_generator.py:13 生成报告时没有把 target_mode /
    evaluation_mode 纳入报告，真实结果和模拟结果还是容易混在一起看。
  - /F:/longchain/第二个项目/llmops-platform/backend/app/services/provider_models.py:204 任何拉取模型列表失败都会静默回退到默认模型或内置列表。这样会掩盖 provider
    故障，实验看起来像“能跑”，但其实可能已经切到了 fallback。
  - /F:/longchain/第二个项目/llmops-platform/backend/app/routers/dashboard.py:18 的健康检查把 Redis 可用 近似当成 Celery ready，但并没有真正验证 worker 是否在线。
    运维界面可能误报“正常”。

  中危

  - /F:/longchain/第二个项目/llmops-platform/backend/app/tasks/celery_app.py:30 到 /F:/longchain/第二个项目/llmops-platform/backend/app/tasks/celery_app.py:72、/
    F:/longchain/第二个项目/llmops-platform/backend/app/tasks/celery_app.py:301 到 /F:/longchain/第二个项目/llmops-platform/backend/app/tasks/celery_app.py:345，以
    及 /F:/longchain/第二个项目/llmops-platform/backend/app/services/classifier.py:79 到 /F:/longchain/第二个项目/llmops-platform/backend/app/services/
    classifier.py:99，都存在很宽的 except Exception: pass 或吞异常逻辑。结果是 Redis、进度推送、分类失败时，系统可能继续往下走，但故障被隐藏，排查很困难。
  - /F:/longchain/第二个项目/llmops-platform/backend/app/main.py:23 启动时直接 Base.metadata.create_all()，没有迁移体系。开发期方便，但一旦 schema 变化，生产升级和
    回滚都不可靠。
  - /F:/longchain/第二个项目/llmops-platform/backend/app/routers/dashboard.py:34 的日志接口会把整个日志文件 readlines() 一次性读进内存，再切尾。日志一大就会有不必
    要的内存压力。
  - /F:/longchain/第二个项目/llmops-platform/backend/app/routers/reports.py:13 和 /F:/longchain/第二个项目/llmops-platform/backend/app/routers/traces.py:62 还在用
    Query.get()，这是 SQLAlchemy 里的旧接口。现在不一定立刻坏，但已经是维护债务，也会持续产生日志警告。